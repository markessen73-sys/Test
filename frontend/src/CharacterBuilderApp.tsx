import { CharacterBuilderView } from './character-builder/CharacterBuilderView';

/** Standalone entry for ?builder=character */
export function CharacterBuilderApp() {
  return <CharacterBuilderView />;
}

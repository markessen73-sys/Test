# Build a Face assets

- `blank.png` — base gym face (no hair), 1024×1024, transparent BG
- `hair/*.png` — hair styles, same colour `#2a1c16`, transparent BG, aligned to blank
- `catalog.json` — index of styles

All hair layers are designed to composite over `blank.png`.
